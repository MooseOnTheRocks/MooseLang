let
    (#push "hello0"; #force print);
    let x = 3 in
    let y = #thunk (
        (#push "hello1"; #force print);
        \z: Number ->
        (#push "we just popped"; #force print);
        (let (#push z; #force num2str) = zs in (#push zs; #force print));
        #push z;
        #push x;
        #force (+)
    )
    in (#push "hello2"; #force print);
    (
        (#push "hello3"; #force print);
        #push 7;
        (#push "we just pushed 7"; #force print);
        #force y
    )
= w in (
    (#push "w is bound to"; #force print);
    (let (#push w; #force num2str) = ws in (#push ws; #force print));
    #push w;
    #push 5;
    #force (+)
)
