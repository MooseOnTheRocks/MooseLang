let w = (1.0, 2.0) in
case w (
    of (x, y) in do (
            do (
                #push x
                #force +
            ) = __app_0
            in
            #push y
            #force __app_0
        ) = sum
        in
        do (
            #push sum
            #force num2str
        ) = __arg_1
        in
        #produce (x, y, sum, __arg_1)
)
